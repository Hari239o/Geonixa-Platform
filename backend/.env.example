# Backend environment variables
PORT=4000
NODE_ENV=development
# Example: MONGODB_URI=mongodb+srv://user:pass@cluster.example.com/dbname
MONGODB_URI=
# Example SMTP settings for server-side email sending
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
